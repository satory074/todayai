# X (Twitter) API — OAuth 2.0 (basecamp と同じ値を流用可)
X_CLIENT_ID=
X_CLIENT_SECRET=
X_REFRESH_TOKEN=
# refresh token のローテーションを GitHub Secrets に書き戻すための PAT（CIのみ。ローカルは不要）
GH_PAT=

# Feedly API — 開発者アクセストークン（30日で失効）
FEEDLY_API_TOKEN=
# 任意: トークン自動更新用のリフレッシュトークン
FEEDLY_REFRESH_TOKEN=
