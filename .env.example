# X API — App-only Bearer Token（外部アカウントのポスト取得用。任意。未設定ならそのソースをスキップ）
# 自分のブックマークは basecamp 公開JSONを読むためトークン不要。
X_BEARER_TOKEN=

# Feedly はトークン不要（公開 RSS を直接取得する方式に変更）。
# 取り込む RSS の URL は feeds.config.ts の feedly.rssUrls に列挙する。
# はてブも公開 RSS でトークン不要。
