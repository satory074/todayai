name: Update feeds & Deploy

on:
  schedule:
    - cron: "0 */6 * * *" # 6時間ごと
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write # feed.json のコミット用
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # 取得＋コミットは cron / 手動実行時のみ（push 時は既存キャッシュでビルドしてループ回避）
      - name: Aggregate feeds
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: npm run aggregate
        env:
          X_CLIENT_ID: ${{ secrets.X_CLIENT_ID }}
          X_CLIENT_SECRET: ${{ secrets.X_CLIENT_SECRET }}
          X_REFRESH_TOKEN: ${{ secrets.X_REFRESH_TOKEN }}
          FEEDLY_API_TOKEN: ${{ secrets.FEEDLY_API_TOKEN }}
          GH_PAT: ${{ secrets.GH_PAT }}
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Commit feed.json if changed
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "feed-bot"
          git config user.email "feed-bot@users.noreply.github.com"
          git add src/data/feed.json
          if git diff --staged --quiet; then
            echo "変更なし"
          else
            git commit -m "chore: update feed $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          fi

      - name: Build site
        run: npm run build
        env:
          GH_USER: ${{ github.repository_owner }}

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
